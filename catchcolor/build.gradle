buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2'  // Changed from 8.11.1
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}